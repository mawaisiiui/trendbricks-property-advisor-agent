# Copy this file to .env and fill in your real values.
# Never commit the real .env file.

DB_HOST=localhost
DB_PORT=3306
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=trendbricks
DB_TABLE=properties

# Only needed once we wire up the LLM agent (later day)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
